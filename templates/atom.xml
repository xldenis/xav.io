<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="{{ lang }}">
    <title>{{ config.title }}
    {%- if term %} - {{ term.name }}
    {%- elif section.title %} - {{ section.title }}
    {%- endif -%}
    </title>
    {%- if config.description %}
    <subtitle>{{ config.description }}</subtitle>
    {%- endif %}
    <link rel="self" type="application/atom+xml" href="{{ feed_url | safe }}"/>
    <link rel="alternate" type="text/html" href="
        {%- if section -%}
            {{ section.permalink | safe }}
        {%- elif term -%}
            {{ term.permalink | safe }}
        {%- else -%}
            {{ config.base_url | safe }}
        {%- endif -%}
    "/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    <updated>{{ last_updated | date(format="%+") }}</updated>
    <id>{{ feed_url | safe }}</id>
    {%- for page in pages %}
    {%- if not page.extra.unlisted %}
    <entry xml:lang="{{ page.lang }}">
        <title>{{ page.title }}</title>
        <published>{{ page.date | date(format="%+") }}</published>
        <updated>{{ page.updated | default(value=page.date) | date(format="%+") }}</updated>
        {%- if page.authors %}
        {%- for author in page.authors %}
        <author>
            <name>{{ author }}</name>
        </author>
        {%- endfor %}
        {%- elif config.author %}
        <author>
            <name>{{ config.author }}</name>
        </author>
        {%- endif %}
        <link rel="alternate" type="text/html" href="{{ page.permalink | safe }}"/>
        <id>{{ page.permalink | safe }}</id>
        {%- for taxonomy_name, taxonomy in page.taxonomies %}
        {%- for taxonomy_term in taxonomy %}
        <category term="{{ taxonomy_term }}" scheme="{{ get_taxonomy_url(kind=taxonomy_name, lang=lang) | safe }}"/>
        {%- endfor %}
        {%- endfor %}
        {%- if page.summary %}
        <summary type="html">{{ page.summary }}</summary>
        {%- endif %}
        <content type="html" xml:base="{{ page.permalink | safe }}">{{ page.content }}</content>
    </entry>
    {%- endif %}
    {%- endfor %}
</feed>
